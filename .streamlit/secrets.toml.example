# Copy this to secrets.toml and fill in your values
# For Streamlit Cloud, add these in the app settings

# Modal credentials (for calling the render backend)
MODAL_TOKEN_ID = "ak-your-token-id"
MODAL_TOKEN_SECRET = "as-your-token-secret"

# Optional: OpenAI for venue setup wizard
OPENAI_API_KEY = "sk-your-key"

# Optional: Replicate for depth estimation
REPLICATE_API_TOKEN = "r8_your-token"
